#include <stdio.h> 

int main (){
    int n,a,b;
    printf("enter the number :");
    scanf("%d",&n);
    for(a=2;a<n;a++)
    { (n%a==0);
       b=1;
    }
    if(b=1){
        printf("the numbr is not a prime numbr");
    }
    else{
        printf("the numbr is a prime numbr");
    }
    return 0;
}